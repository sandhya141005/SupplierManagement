@model List<SupplierManagement.Web.Models.DTO.UserDTO>
<h2>Users</h2>

<table>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.FirstName</td>
            <td>@user.Email</td>
        </tr>
    }
</table>